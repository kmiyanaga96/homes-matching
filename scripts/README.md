# Scheduler simulator

Simple Node.js prototype to test the greedy scheduling heuristic for studio allocation.

Usage:

```bash
node scripts/scheduler_simulator.js
```

Output: `scripts/schedules_output.json`
